function QuestionTitle({ title }) {
  return (
    <div className="p-6 mx-auto bg-white items-center space-x-4">
      <p className="text-slate-500">{title}</p>
    </div>
  );
}

export default QuestionTitle;
